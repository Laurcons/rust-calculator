[package]
name = "rust"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["exclusive_range_pattern"]
exclusive_range_pattern = []

[dependencies]
termion = "3.0.0"
